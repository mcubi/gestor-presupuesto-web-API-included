# QUICK RUNNING GUIDE:

## Step 1: ```cd api``` + ```npm install``` + ```npm start```
## Step 2: ```cd ../web``` + open index.html trhough live server. 
---
## WARNING! If you open the file through file:/// (opening directly) You may have CORS errors. Better to have installed live server and execute it
